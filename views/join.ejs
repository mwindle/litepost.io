<!DOCTYPE html>
<html lang="en">
	<head>
		<title>Live Blogging Application</title>
		<% include partials/stylesheets %>
	</head>
	<body>
		<% include partials/top-bar %>
	  <div class="main-content">
		  <form role="form" class="login-form" method="post">
		  	<header>
			  	<h2>
			  		<span class="emphasize">Join</span>
			  	</h2>
			  </header>
			  <% if(locals.errors) { %>
			  <div class="alert alert-danger" role="alert">Unable to create account. Please fix any errors below and try again. </div>
			  <% } else if(locals.serverError) { %>
			  <div class="alert alert-warning" role="alert">Oops! Something went wrong on the server and we couldn't process your request. Please try again in a few moments. </div>
			  <% } %>
		  	<div class="form-group">
		  		<label for="email">Email</label>
		  		<input type="email" class="form-control" id="email" name="email" placeholder="Email" value="<%= locals.email || '' %>" autofocus />
		  		<% if(locals.errors && locals.errors.duplicate) { %>
		  		<span class="text-danger">An account already exists with that email. </span> 
		  		<span>Try <a href="/login?e=<%= locals.email || '' %>">signing in</a> instead?</span>
		  		<% } %>
		  	</div>
		  	<div class="form-group">
		  		<label for="password">Password</label>
		  		<input type="password" class="form-control" id="password" name="password" placeholder="Password" />
		  	</div>
		  	<div class="form-group">
		  		<label for="confirm-password">Confirm password</label>
		  		<input type="password" class="form-control" id="confirm-password" name="confirmPassword" placeholder="Confirm" />
		  	</div>
		  	<button type="submit" class="btn btn-primary login-btn">Create account</button>
		  </form>
			<% include partials/footer %>
		</div>
	</body>
</html>